dist: xenial
language: python

python:
  - "3.6"
  - "3.7"
  - "3.8"
  - "3.9"
  - "pypy"

install:
  - pip list & pip install -U setuptools && pip install -U tox-travis

script:
  - tox

after_success:
  - bash <(curl -s https://codecov.io/bash)
